// Jenkinsfile (Declarative Pipeline)
pipeline {
    agent { docker 'golang:1.6' }
    stages {
        stage('build') {
            steps {
                sh 'cd server/experiment_server && make build'
            }
        }
        stage('deploy') {
            steps {
                sh 'cd server/experiment_server && make deploy'
            }
        }
    }
}