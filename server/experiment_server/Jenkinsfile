// Jenkinsfile (Declarative Pipeline)
pipeline {
    agent { docker 'golang:1.6' }
    stages {
        stage('build') {
            steps {
                sh 'make build'
            }
        }
        stage('deploy') {
            steps {
                sh 'make deploy'
            }
        }
    }
}