Jenkinsfile (Declarative Pipeline)
pipeline {
    agent { docker 'grpc/go:1.0' }
    stages {
        stage('build') {
            steps {
                sh 'go version'
            }
        }
    }
}
